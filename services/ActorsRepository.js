/* import { db } from '../config/config_bbdd';
import { collection, onSnapshot, query, querySnapshot } from 'firebase/firestore';


interface ActorsRepository {
    getActors(): Promise<any>,
}

export const actorsRepository: ActorsRepository = {
    function getActors() {

    }
} */